# Firefly Docker Configuration
# Copy this file to .env and modify values as needed

# =============================================================================
# Application Configuration
# =============================================================================

# Application name displayed in UI
FIREFLY_NAME=Firefly

# Web server configuration
FIREFLY_WEB_PORT=5011
FIREFLY_DEBUG=false

# Security - CHANGE THIS IN PRODUCTION!
FIREFLY_SECRET_KEY=dev-secret-key-change-in-production

# =============================================================================
# UDP Configuration for Meshtastic Communication
# =============================================================================

# UDP server settings for Meshtastic device communication
FIREFLY_UDP_HOST=0.0.0.0
FIREFLY_UDP_PORT=4403

# =============================================================================
# Docker Image Configuration
# =============================================================================

# Uncomment to use a pre-built image instead of building locally
# FIREFLY_IMAGE=ghcr.io/pdxlocations/firefly:latest

# =============================================================================
# Development Settings
# =============================================================================

# Set to true for development mode (more verbose logging, auto-reload)
# FIREFLY_DEBUG is already defined above in Application Configuration

# =============================================================================
# Example Production Configuration
# =============================================================================

# For production, you might want to:
# 1. Change FIREFLY_SECRET_KEY to a strong, unique value
# 2. Set FIREFLY_DEBUG=false
# 3. Consider changing default ports if needed
# 4. Ensure proper firewall configuration for UDP_PORT (4403 for Meshtastic)

# Generate a secure secret key with:
# python3 -c "import secrets; print(secrets.token_hex(32))"